package com.example;

public class App {
    public static void main(String[] args) throws Exception {
        new login().createWindow();
        // UserInterface tmp = new UserInterface("sw710407@gmail.com");
        // // File file = new File("demo\\src\\main\\java\\com\\example\\dog.png");
        // // BufferedImage bufferedImage = ImageIO.read(file);
        // // tmp.updateOneUserData("DC", "HI", "PASS", bufferedImage);

        // List<RecordData> finalResult = tmp.fetchAllUserData();

        // BufferedImage bufferedImage = finalResult.get(0).getImage();
        // // 建立 JFrame 物件作為圖片顯示視窗
        // JFrame frame = new JFrame("Image Display");
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // // 建立 JLabel 並設定圖片
        // JLabel label = new JLabel(new ImageIcon(bufferedImage));

        // // 將 JLabel 加入 JFrame
        // frame.getContentPane().add(label);

        // // 設定 JFrame 的大小
        // frame.setSize(bufferedImage.getWidth(), bufferedImage.getHeight());

        // // 顯示 JFrame
        // frame.setVisible(true);
    }
}
